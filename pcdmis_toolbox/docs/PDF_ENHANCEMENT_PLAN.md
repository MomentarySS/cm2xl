# PDF 扫描提取 → Excel 增强计划

> 目标：提升 `cmm_filler` 模块 PDF 识别准确率与适用范围，同时保持离线打包约束（600–700MB）。

---

## 现状问题

| 问题 | 影响 |
|------|------|
| 只处理 PDF 第 1 页 | 多页报告数据丢失 |
| 纯 OCR 路径，不用文本层 | 文字型 PDF 准确率与速度均差 |
| 按行拼接 OCR 结果，无坐标对齐 | 列错位是主要失败原因 |
| 解析强绑定 FAI 版式 | 换厂商报告易失效 |
| 样品位/行数硬上限 | 大批量样品被截断 |
| 无 PDF 专项测试 | 改解析规则易回归 |

---

## Phase 1 — 提取层加固（1–2 天）✅ 已完成

| 步骤 | 内容 | 状态 |
|------|------|------|
| 1.1 | PDF 类型检测（`get_text` 快速通道 vs OCR） | ✅ |
| 1.2 | 多页全量扫描（遍历所有页） | ✅ |
| 1.3 | OCR bbox 坐标表格对齐解析 | ✅ |
| 1.4 | 统一 `extract_pdf_data()` 入口 + 单元测试 | ✅ |

**交付物：**
- `core/pdf_extract.py` — 类型检测、文本提取、多页渲染
- `ocr/engine.py` — `OCRBox` + `recognize_detailed()`
- `core/parse_measurements.py` — 坐标表格解析
- `tests/test_pdf_extract.py` — 纯函数回归测试

---

## Phase 2 — 解析与交互增强（2–3 天）✅ 已完成

| 步骤 | 内容 | 状态 |
|------|------|------|
| 2.1 | 表格 ROI 裁剪配置（只 OCR 测量数据区域） | ✅ |
| 2.2 | 报告模板 Profile 配置化（`report_profiles/*.json`） | ✅ |
| 2.3 | GUI OCR 预览修正（低置信度标红 + 手动编辑） | ✅ |
| 2.4 | 提取来源标记（text / ocr-table / ocr-line）写入日志 | ✅ |

---

## Phase 3 — 功能扩展（按需）✅ 已完成

| 步骤 | 内容 | 状态 |
|------|------|------|
| 3.1 | NG 统计分析导出（集成 NG 排行能力） | ✅ |
| 3.2 | 样品位动态扩展（多 Sheet 自动分块） | ✅ |
| 3.3 | pdfplumber 通用表格导入（可选依赖） | ✅ |
| 3.4 | 单元测试套件扩展 | ✅ |

---

## Phase 4 — 回归验证与标签解析巩固 ✅ 已完成

| 步骤 | 内容 | 状态 |
|------|------|------|
| 4.1 | 多前缀 / 多子编号格式解析（FAI、CC、DIM 等） | ✅ |
| 4.2 | 用户可配置前缀（额外前缀 + 自定义 Profile） | ✅ |
| 4.3 | 真实 PDF 端到端回归（Desktop/test 4 份） | ✅ |
| 4.4 | OCR 表格对齐命中率优化（减少 ocr-line 回退） | ✅ |

### Phase 4 验收
- [x] `FAI_34-1` / `FAI_34-2` 独立识别，49 项无重复键
- [x] CC / DIM 等多格式标签单元测试（36 项通过）
- [x] 4 份测试 PDF 重新导出 Excel + NG 统计
- [x] ocr-table 路径命中率提升（4 份测试 PDF 全部走 ocr-table）

---

## Phase 5 — 模板写入与子编号冲突 ✅ 已完成（v1.0.7）

| 步骤 | 内容 | 状态 |
|------|------|------|
| 5.1 | 默认按模板 A 列序号定位写入（`fixed_page_layout`） | ✅ |
| 5.2 | 样品数超模板列数时自动拆多个 Excel（`_样品1-N.xlsx`） | ✅ |
| 5.3 | PC-DMIS 专项：公差归一化、多轴偏好、序号间隙推断 | ✅ |
| 5.4 | 子编号冲突检测（`FAI_1-1`…`FAI_1-N` → 模板序号 `1`） | ✅ |
| 5.5 | 预览高亮 + 动态选项（各子项 / 都不填 / 较差 NG） | ✅ |
| 5.6 | 单元测试扩展至 58 项（cmm_filler）/ 158 项（全项目） | ✅ |

### Phase 5 验收
- [x] 多子项（2～N 个）冲突选项动态生成，不写死样本数
- [x] 仅 1 个子项映射父序号时自动填入，无需用户选择
- [x] CLI 无预览时默认「较差(NG)项」；GUI 预览可逐组覆盖
- [x] `overflow_samples=true` 仍走旧版 Sheet 复制逻辑（与子编号冲突选择独立）

### 已知边界
- **overflow_samples 模式**不使用模板序号回退与子编号冲突解析（保留旧行为）
- **CLI 批量处理**无交互预览，冲突默认取 NG 最严重子项
- **多轴特性（圆柱 X/Y/D）**：当前每个 CC 序号只提取并写入 **一个** 实测值（见 Phase 6）

---

## Phase 6 — 多轴拆行（待开发）

> 记录于 2026-08-29。当前业务以取 **D 轴（直径）** 为主，已满足出货表需求；本 Phase 供后续模板需分行填写时实现。

### 背景

PC-DMIS 等报告中，部分特性（如 `CC_15 - 圆柱1`）在 AX 表下含 **多行数据**（X、Y、D 等）。用户 Excel 模板也可能为同一序号预留 **多行**（例如序号 15 占 3 行）。

### 现状（v1.0.9）

| 环节 | 行为 |
|------|------|
| PDF 解析 | 每个 CC/FAI 序号 → **一条** measurement；从 X/Y/D 中按优先级或模板「轴」列取 **一轴** |
| 轴选择 | 模板「轴」列填 `D`/`X`/`Y` 等 → 指定取哪一轴；**未配置时默认 D**；角度描述优先 **A** |
| OCR 漏标签 | 相邻已识别序号之间按 **AX 表头分块** 补全缺口，多缺口不共用第一块 |
| Excel 写入（CMMFiller） | 序号列键 **唯一**，只写入该序号 **首次出现** 的行；其余预留行保持空白 |
| 出货表填入（pc_to_excel） | 同样每序号一格，多轴优先 **D**，XYZ 仅参考（`inspection_form_fill._pick_form_fill_row`） |

相关代码：`parse_measurements._pick_primary_ax_nums`、`_collect_ax_blocks_between`、`fixed_page_layout.load_axis_preferences_from_template`、`build_sheet_row_index`、`inspection_form_fill._pick_form_fill_row`。

### 目标行为（规划）

1. 模板多行：同一序号（或首行序号 + 后续空序号续行）+ 各行「轴」列分别标 `X`/`Y`/`D`
2. PDF 解析：从多轴 AX 块拆出多条记录（或单条记录带 `axes: {X:…, Y:…, D:…}`）
3. 写入：按 `(序号, 轴)` 复合键定位行，分别填入对应实测值、公差
4. 预览：拆行结果在预览窗口展示，低置信度/NG 按行标色
5. **向后兼容**：仅配置单轴或未配轴列时，保持现有「取 D、写一行」逻辑

### 实现要点（草案）

- [ ] `build_sheet_row_index` 支持 `(serial_key, axis)` → `(sheet, row)`，同序号多行不互相覆盖
- [ ] `parse_from_ocr_boxes` 可选输出 `multi_axis` 结构或按轴展开为多条 measurement
- [ ] `resolve_measure_write_location` 匹配时同时考虑序号与轴
- [ ] 单元测试：`CC_15` 三行 PDF + 模板三行 → X/Y/D 各写入正确单元格
- [ ] 文档：`cmm_filler.md` 补充多轴模板配置说明

### 非目标（本期不做）

- 不改变「单序号只关心直径」的默认策略（仍默认 D）
- 不与「子编号冲突」（`FAI_1-1`…`FAI_1-N`）混用同一套 UI；多轴是同一特性的不同测量轴，语义不同

---

## 技术约束

- **离线环境**：不新增默认打包依赖，PaddleOCR 保持主引擎
- **向后兼容**：`parse_from_text()` 保留为 fallback
- **缓存**：PDF 级缓存 key = `pdf:{path_md5}`，与图片 OCR 缓存分离
- **性能**：文字型 PDF 跳过 OCR，速度提升 10×+

---

## 验收标准

### Phase 1
- [x] 文字型 PDF 走 `get_text()` 不调用 OCR
- [x] 多页 PDF 合并所有页内容后解析
- [x] 扫描件优先 bbox 表格解析，失败回退行解析
- [x] 现有 GUI/CLI 三条导出路径均通过新入口
- [x] 单元测试覆盖类型检测、行聚类、解析逻辑

### Phase 2
- [x] 可在设置中配置 ROI 区域
- [x] 至少支持 2 种报告 Profile 切换
- [x] GUI 预览可手动修正误识别项

### Phase 3
- [x] NG 统计 Excel 导出（GUI「NG 分析」页 + CLI `ng-stats`）
- [x] 样品超限时自动创建多个溢出 Sheet（不再截断丢弃）
- [x] pdfplumber 通用表格提取（可选，`pip install pdfplumber`）
- [x] NG 统计 / 样品分块单元测试
