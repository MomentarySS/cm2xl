---
description: Python 模块、GUI、PC-DMIS COM 与测试约定
globs: pcdmis_toolbox/**/*.py
alwaysApply: false
---

# Python 模块开发

- 模块适配层底部：`register_module("<name>", ModuleClass())`（见 `modules/__init__.py`）
- GUI 线程：长任务用 `utils.threading_utils.CancellableWorker`，`daemon=True`
- PCDMIS：COM 常量用 `get_const()`；`prog_id` 由 `PcdmisConnector.connect()` 写入 `_ACTIVE_PROG_ID`
- 模块发现：打包后不能用 `Path.is_file()` 判断 `gui.py`，用 try-import（见 CLAUDE.md §5）
- tkinterdnd2：独立与挂载模式都要 `TkinterDnD._require()`
- CustomTkinter：`CTkLabel` 的 `fg_color` 须 `transparent`；`CTkFont` 必须含 `weight`
- 测试：模块内 `tests/` 用 pytest；改 `cmm_filler` / `pc_to_excel` 核心逻辑后跑对应用例
- 最小 diff：只改与任务相关的模块，不顺手重构无关代码
