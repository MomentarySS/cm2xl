---
description: cm2xl / PC-DMIS 工具箱项目全局约定
alwaysApply: true
---

# cm2xl（a1 / pcdmis_toolbox）全局约定

- 主代码在 `pcdmis_toolbox/`；架构真源见 `ARCHITECTURE.md`，日常开发约定见 `pcdmis_toolbox/CLAUDE.md`
- 目标：CMMFiller + pc_to_excel 统一工具箱；模块可挂载 Shell，也可独立运行
- 环境：**64 位 Python 3.12+**、Windows、测量房**无网**；依赖须可打包（约 600–700MB）
- 导入：项目内用**0 连点**（`from utils.paths import paths`），禁止 `from ....utils` 越界相对导入
- `main.py` 顶部：`multiprocessing.freeze_support()` 必须在所有 import 之前
- 主题：`apply_theme()` 只在 `main.py` 调一次；颜色从 `utils.theme.TOOLBOX_THEME` 取，禁止硬编码
- 配置写：用 `utils.settings.save_settings_json_atomic()`；PDF 用 `utils.file_io.fitz_open_context()`
- 新功能按职责放进 `modules/pc_to_excel/core/` 子模块，不要再堆回 `data_extractor.py` god-file
- 改打包 / OCR / PyInstaller 时对照 `ARCHITECTURE.md` 离线部署约束与 `build/fix_dist.py`
- 仅在我明确要求时 git commit；不自动 push
